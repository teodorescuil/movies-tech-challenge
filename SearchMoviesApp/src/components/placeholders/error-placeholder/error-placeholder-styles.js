import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingHorizontal: 16,
        marginTop: 24
    },
    title: {
        fontSize: 16,
        fontWeight: 'bold',
        textAlign: 'center',
        color: 'red'
    },
    subtitle: {
        fontSize: 16,
        textAlign: 'center',
        color: 'grey'
    }
});
