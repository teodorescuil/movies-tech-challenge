import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingHorizontal: 16
    },
    placeholder: {
        fontSize: 16,
        fontWeight: 'bold',
        textAlign: 'center'
    }
});
